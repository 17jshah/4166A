<%- include('../partials/header.ejs')%>
    <div class = "content">
        <h4><%= story.title%></h4>
        <small>posted <%= story.createdAt%> by <%=story.author%> </small>
    <div id = "story"><%= story.content%></div>
<form>
    <button type ="submit" formmethod= "Get" formaction = "/stories/<%= story.id%>/edit">Edit</button>4
    <button type ="submit" formmethod= "POST" formaction = "/stories/<%= story.id%>?_method=DELETE">Edit</button>

</form>    
</div>
    <%- include('../partials/footer.ejs')%>